# 2026-05-14 工作日志

## 完成内容
1. **CurrencyManager（Autoload）**：全局货币管理，add/spend/has_enough，changed 信号
2. **组件化重构**（core/components/）：
   - HealthComponent: 血量组件（从 Target/Enemy 提取），hp/max_hp/take_damage/heal/get_ratio
   - ShootingComponent: 射击组件（从 Target 提取），AUTO/MOUSE 瞄准 + 自动射击
   - MoveComponent: 移动组件（从 Enemy 提取），向目标移动 + is_in_range 判断
   - AttackComponent: 攻击组件（从 Enemy 提取），try_attack() 定时攻击
3. **Target 重构**：挂载 HealthComponent + ShootingComponent，代理 take_damage
4. **Enemy 重构**：挂载 HealthComponent + MoveComponent + AttackComponent，代理 take_damage
5. **InventoryUI 增强**：
   - 代码构建货币显示（💰）在左侧标题下方
   - 代码构建售卖区域：−/+ 数量选择 + 售卖/全部售卖按钮
   - 售卖逻辑：CurrencyManager.add + InventoryManager.remove_item
6. **project.godot**：注册 CurrencyManager Autoload
7. **InventoryUI 大改**：
   - UI 全部改为 .tscn 构建（不用代码创建），脚本只管逻辑
   - 背包改为居中窗口式（类似我的世界），DimBg + CenterContainer + WindowPanel
   - 售卖内嵌在右侧详情下方：HSlider 拉条选数量 + 确认售卖按钮
   - 所有非交互节点 mouse_filter = IGNORE，只有按钮/滑条保留 STOP
8. **main.tscn 修复**：
   - 根节点 Control → Node
   - 新增 CanvasLayer(UILayer) 放所有 UI，不受相机偏移
   - 移除 StatusEffectBar 的全屏覆盖（这是按钮点不了的根因！）
9. **鼠标事件修复**：
   - **根因**：main.tscn 中 StatusEffectBar 被覆盖为 anchors_preset=15（全屏），其 mouse_filter 默认 STOP，排在 InventoryUi 上面吞掉所有点击
   - 修复：移除 StatusEffectBar 的全屏覆盖 + 所有非交互 Control 设 mouse_filter=2(IGNORE)
   - 教训：UILayer 中的每个全屏/大面积 Control 都必须设 mouse_filter = IGNORE
10. **战斗系统 Bug 修复**：
    - **根因**：MoveComponent.stop_distance=30 < 双方碰撞半径之和(20+16=36)，敌人永远无法进入攻击范围
    - 修复：stop_distance 30 → 50（必须大于碰撞半径之和）
    - 教训：stop_distance 必须大于碰撞体半径之和
11. **GameOver 按 R 无反应修复**：
    - **根因**：`_unhandled_input` 在 `get_tree().paused = true` 时不触发
    - 修复：改 `_input` + `process_mode = PROCESS_MODE_ALWAYS`
12. **PlayerStats 属性系统**（core/components/player_stats.gd）：
    - 初始属性: 750HP, 20ATK, 25%CRIT, 50%CD, 10DEF
    - 防御公式: 每10点防御 受伤-1，下限0
    - 效果加成: 遍历 EffectManager.get_active() 汇总
    - 暴击: randf() < crit_rate → 伤害 × (1 + crit_damage)
13. **Target 重构**:
    - 挂载 PlayerStats，max_hp/伤害/射速 全部由属性驱动
    - take_damage 先经 calc_damage_taken（防御减伤）
    - stats_changed 信号同步 HealthComponent 和 ShootingComponent
    - 加入 "targets" 组供 UI 查找
14. **Bullet 改造**:
    - damage 不再硬编码，由 ShootingComponent 从 PlayerStats.calc_damage_dealt() 设置
    - is_crit 标记暴击
15. **StatsPanel**（右下角属性面板）:
    - .tscn: PanelContainer 锚点右下角 170×120
    - .gd: 绑定 Target 的 PlayerStats + HealthComponent，stats_changed 时刷新
    - 显示: ❤HP/⚔ATK/🛡DEF/💥CRIT/💥CD/🔥射速
16. **射速语义改造**: fire_rate 从"秒/次"(0.5) → "次/秒"(1.0)
17. **波次系统+倒计时**: EnemySpawner 每30s递增wave，WaveDisplay显示"第N波 30s"
18. **怪物数值平衡**: HP=50*(level*1.1), ATK=2+level, SPD=60+level*3, 攻击间隔1.5s
19. **玩家初始属性改为**: 100HP/10ATK/10DEF/5%CRIT/25%CD/1.0射速
20. **升级系统**:
    - PlayerStats.UPGRADE_CFG: base/per_level/base_cost
    - 升级费用: base_cost × 1.5^current_level
    - StatsPanel 每属性行加"💰N"升级按钮，金币不足禁用
    - 升级反馈: +50HP/+5ATK/+3DEF/+3%CRIT/+15%CD/+0.3射速
21. **怪物掉落**:
    - 金币: level * 3
    - 物品: 25%+level*2%概率(上限50%)，加权随机从5种物品中选
22. **WaveConfig外部配置系统**（core/base/wave_config.gd + data/wave_config.tres）:
    - Resource 类，所有波次/怪物/掉落数值为 @export 可在编辑器调整
    - 三阶段难度: 轻松(1-3)/普通(4-10)/困难(11+)
    - 方法: get_enemy_hp/atk/speed/attack_interval, get_spawn_interval, get_drop_chance
23. **玩家HP回退100**，基础ATK=15, DEF=10
24. **EnemySpawner改造**: 使用WaveConfig替代硬编码，spawner自动加载默认config
25. **Enemy改造**: 从WaveConfig读取属性而非硬编码，wave_config由Spawner注入
26. **DropIndicator**（scenes/world/drop_indicator.gd）:
    - 静态方法show()，怪物死亡时浮动显示金币+物品名（品质色）
    - 浮动1.5s后淡出自动释放
27. **WaveDisplay增强**: 显示难度阶段(轻松/普通/困难) + 对应颜色(绿/黄/红)
28. **掉落概率提升**: 基础40%+每波3%，上限80%（旧:25%+2%，上限50%）
29. **金币掉落提升**: level*5（旧: level*3）
22. **数值平衡(3波不死)**:
    - 玩家: HP=500, ATK=15, DEF=10
    - 怪物: ATK=1+level, 攻速=2.0s, 速度=50+level*2
    - 生成: 间隔3.0s起步, -0.15/波, 最低1.0s, 上限15只
