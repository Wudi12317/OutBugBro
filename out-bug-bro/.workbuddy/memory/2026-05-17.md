# 2026-05-17 工作日志

## 修复汇总（连续Bug修复）
- `inventory_ui.gd` L295: `var key_str :=` 三元表达式无法推断 → `: String =`
- `target.gd` 多处 `:=` 推断失败（charging/progress/ring_size/glow_size/orbit_r/angle等）→ 全部显式类型
- `default_drop_table.tres` 手写格式解析失败（Extra tag）→ 用正确 Godot 格式重建（ext_resource 引脚本+物品，sub_resource 定义 DropEntry），enemy.gd 优先 load .tres，失败才 fallback 程序化构建
- `shooting_component.gd` L224: `var orig :=` → `: Vector2 =`
- `skill_bar.gd` L306-309: `item_keys/key_str/tooltip` 误放在 `if data:` 块外 → 提到if前，tooltip分支内赋值
- `shooting_component.gd`: `var orig := camera.position` → `: Vector2`
- `enemy.gd`: HealthComponent 缺少 `entity = self` 导致 Nil 访问
- `bullet.gd`: `direction` 永远是 `Vector2.RIGHT`，加 `_ready()` 中 `direction = Vector2.RIGHT.rotated(rotation)`
- `shooting_component.gd`: 伤害始终初始值 → 给组件加 `stats` 引用，射击时 `stats.calc_damage_dealt()`
- 右键左键都可射击 → `_process_charge_input` 加 `MOUSE_BUTTON_RIGHT` 判断
- `target.gd`: `_shooting.enabled = false` → `set_physics_process(false)`
- 主菜单 OverwriteDialog 按钮文字改为"继续游戏"，逻辑改为加载存档
- 手动模式瞄准不受转速影响 → MOUSE模式直接 `entity.rotation = target_angle`

## 性能优化
- `bullet.gd` 拖尾改用静态对象池（80个ColorRect），间隔0.05s（原0.02s减60%）
- `enemy.gd` 伤害数字改用静态对象池（20个Label）
- `enemy_spawner.gd` 用 `_enemies_alive` 计数器替代每帧 `get_nodes_in_group()`，通过 `enemy.died` 信号维护

## 新增文件
- `PROJECT_SUMMARY.md`：项目总览文档（架构/系统/UI/性能/踩坑）
