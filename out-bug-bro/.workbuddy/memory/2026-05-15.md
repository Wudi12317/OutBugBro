# 2026-05-15 工作日志

## 完成内容
1. **项目全面复盘审计**：检查17个维度，发现10项已完成、2项部分完成、5项未完成
2. **D/E 死代码清理**：
   - 从 EffectData 中移除 `item_prob_change` 和 `hp_regen_change`（未被 PlayerStats 消费，无 .tres 使用）
   - 从 get_active_effects() 和 StatusEffectBar._attr_name() 同步清理
   - 新增 `move_speed_change` 字段替代（已被 PlayerStats.get_move_speed() 消费）
3. **H 修复**：TargetHpBar .tscn 硬编码 500 → 0/0（运行时由事件动态设置）
4. **M 新增 move_speed 属性**：
   - PlayerStats: 新增 move_speed 到 STAT_INFO、upgrade_levels、_cfg_base/per_level/base_cost、get_move_speed()
   - PlayerConfig: 新增 move_speed/move_speed_per_level/move_speed_base_cost
   - EffectData: 新增 move_speed_change（替代移除的字段）
5. **策划数据修改指南**：创建 DATA_GUIDE.md，详列所有 .tres 文件字段含义、当前值、调参建议
