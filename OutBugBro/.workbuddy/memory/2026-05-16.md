# 2026-05-16 工作日志

## 修复
- DropIndicator.set_deferred 时序 bug：add_child 立即触发 _ready，改为 add_child 前直接赋值
- DropIndicator.show() → spawn()：避免与 CanvasItem.show() 冲突

## 新增系统
- **掉落系统完善**：品质视觉反馈（传说光柱/史诗脉冲/稀有闪烁）、DropTable Resource 配置化、掉落率显示
- **波次系统完善**：FIGHTING/BREAK 状态机、5秒休息期、清波奖励(wave×100)、WaveAnnounce 大字公告
- **连击系统**：ComboManager(Autoload) 2秒窗口、每级+5%掉率(上限50%)、ComboDisplay 动画
- **精英怪**：10%概率、HP×3、体型1.5倍、金色边框、100%掉落稀有以上

## 新增数据
- 3新效果: power_boost / iron_wall / lucky_aura
- 3新物品: 力量药水 / 铁壁药水 / 幸运符
- 掉落表8种物品

## 文档
- DATA_GUIDE.md 全面重写（新增怪物/物品/效果教程）

## 晚间更新：技能系统 + 节奏 + 掉落修复

### 修复
- 掉落不工作：default_drop_table.tres 手写格式不可靠，改为 _build_default_drop_table() 程序化构建
- AttackComponent: try_attack() 现在传入 entity 作为 attacker 参数（支持反伤）

### 新增系统
- **技能系统**: SkillData(Resource) + SkillManager(Autoload) + SkillBar UI
  - 5技能: 临时护盾/主动防御/应急修理/制导导弹/头油
  - 按键 1-5 激活技能，6-0 使用消耗品
  - 技能购买（波次解锁 + 金币）+ 冷却 + 持续时间
- **MoveComponent增强**: apply_knockback() + apply_slow()
- **Target技能交互**: 护盾免疫/反伤(75%反+80%减)/修理(90%回血+buff)/导弹(清怪)/头油(弹开+减速)

### 节奏调整
- max_enemies: 10→30, spawn_base: 4.0→2.5s, spawn_min: 0.8→0.3s
- enemy_speed_base: 40→60, 各阶段速度提升

### 新增数据
- emergency_buff效果 (+100ATK/+100%倍率/+50%暴击/+100%爆伤/+30DEF)
- 5技能 .tres (shield/reflect/repair/missile/head_oil)

### UI
- SkillBar: 底部10格(5技能+5消耗品快捷槽)
- StatsPanel: emergency_buff激活时属性金色字体
- InventoryUI: 物品详情显示掉落率, 移除F/E测试键

## 晚间更新2：UI修复 + 视觉增强

### Bug修复
- SkillManager `class_name` 与 autoload 冲突 → 移除 class_name
- skill_bar.gd `data.name` → `data.skill_name`（Resource无name属性）
- target.gd `var dir :=` → `var dir: Vector2 =` 类型推断
- InventoryUI 出售/升级按钮点不到 → SkillBar HBoxContainer mouse_filter=IGNORE
- 技能/消耗品无法鼠标点击 → 旧 gui_input 信号不稳定，改用 Button 覆盖整个格子
- 背包面板被遮挡 → 打开时设 z_index=100

### SkillBar 重写
- 格子统一为背包风格：64×64, 品质色边框+半透明底+左上角标
- 技能/消耗品都用 Button 覆盖做点击，flat=true, focus_mode=NONE
- CD倒计时：黑色半透明遮罩从底部填充，中央显示剩余秒数
- 激活中：青色边框+底色，显示剩余时间
- tooltip 显示完整技能信息（名称+按键+描述+冷却）

### 技能视觉特效
- 护盾：脉冲青色光环围绕Target
- 反伤：脉冲紫色光环
- 修理：金色爆发光圈
- 导弹：红橙色大爆发光圈
- 头油：黄绿色小爆发光圈

### 拖尾动效
- TrailComponent（core/components/）: 轻量粒子拖尾，每隔0.03s生成渐隐小方块
- Enemy: 红色拖尾（精英金色），半径4/6
- Bullet: 黄色拖尾，半径2，0.02s间隔

### 踩坑
- `class_name` vs autoload: autoload单例不能再写class_name
- `Resource.name`: Resource没有name属性（Node的），自定义Resource用skill_name等字段
- Button覆盖技巧：flat Button覆盖整个格子做点击，比gui_input信号更可靠

## 晚间修复2: UI交互 + 技能栏视觉 + 传说物品

### Bug修复
- SkillBar HBoxContainer 默认 mouse_filter=STOP 遮挡 StatsPanel 底部3个升级按钮和 InventoryUI 出售区域
  - 修复：SkillBar .tscn 加 mouse_filter=2 (IGNORE)，子按钮仍可交互
- SkillData.name 应为 SkillData.skill_name（Resource 无 name 属性）
- SkillManager class_name 与 autoload 同名冲突，移除 class_name
- target.gd dir 变量类型推断失败，var dir := 改为 var dir: Vector2 =

### 技能栏视觉重构
- 技能槽/消耗品槽改为背包格子同风格：56×56, 品质色背景+边框+图标+文字叠加
- 左上角快捷键角标（1-5, 6-0）
- 消耗品槽右下角数量显示
- 激活时青色背景+边框，冷却时暗灰，未解锁时灰色

### 新增传说(金色/rarity=4)物品
- 龙之心(dragon_heart): 消耗品, +200回血/+30ATK/+30%倍率/+15%暴击, 15s, 1200💰
- 凤凰羽(phoenix_feather): 消耗品, +500回血/+200HP上限/+20DEF, 20s, 1500💰
- 陨星核(meteor_core): 材料, 价值2000💰, 精英怪/高波次掉落
- 对应效果: dragon_fury / phoenix_revive
- 对应图标: dragon_heart.svg / phoenix_feather.svg / meteor_core.svg
- 掉落表权重各1（极稀有）

### 踩坑
- HBoxContainer/VBoxContainer 默认 mouse_filter=STOP，全屏底栏会遮挡后面UI，必须设 IGNORE

## 晚间更新3：主菜单 + 存档 + 死亡界面

### 新增系统
- **SaveManager** (Autoload): JSON存档 `user://save_data.json`
  - save_run()/load_run()/reset_run()/full_reset()
  - calculate_grade(): C→B→A→S→SS→SSS→SSS+→SSS+⭐N
  - _pending_load: 继续游戏标志
- **主菜单** (main_menu.tscn): iOS暗色风+粒子雨60粒子+闪电+呼吸标题+hover放大
  - 5按钮: 开始/继续/介绍/关于/退出
  - 有存档→覆盖(保留最高)/新游戏(红色,重置一切)/取消
  - 介绍/关于弹窗（内容待用户编辑）
- **死亡界面** (death_screen.tscn): 替代原GameOver
  - 评分等级+颜色+炫彩(SSS+⭐) + 击杀/波次 + 重开/回菜单
- 主场景改为 main_menu.tscn
- Target加"player"组, Enemy死时run_kills++, 每波结束自动存档
