# 2026-06-05 工作日志

## 修正项目路径
- 正确路径: `E:\HuaweiMoveData\Users\wudi1\Desktop\OutBugBro\OutBugBro`
- 之前一直在改错误的 `out-bug-bro` 目录，已切换到正确路径

## UI圆角改直角
- 全项目所有 `.gd` 文件的 `corner_radius` 改为 0
- 涉及文件: ui_theme.gd(12→0), main_menu.gd(10/12→0), skill_bar.gd(4/8→0), meta_upgrades.gd(12→0), target_hp_bar.gd(4/5→0), boss_hp_bar.gd(4→0), game_manager.gd(8→0), challenge_mode.gd(12/4→0), drop_indicator.gd(12→0), dev_panel.gd(8→0), effect_slot.gd(3→0), inventory_slot.gd(4→0), enemy.gd(2→0)

## 修复元数据修改时Label不更新bug
- 根因: `MetaProgression.upgraded` 信号发出后，`PlayerStats` 没有监听，导致 `stats_changed` 不触发
- 修复: `player_stats.gd` 的 `_ready()` 中添加 `MetaProgression.upgraded.connect(func(_id): stats_changed.emit())`

## 优化血条视觉效果
- **TargetHpBar**: 动态颜色(>60%绿→30-60%橙→<30%红) + 扣血延迟条(0.5s) + 低血警告脉冲 + 外发光框随血量变色
- **BossHpBar**: 动态红色(>50%亮红→20-50%暗红→<20%深红) + 扣血延迟条 + 低血(<20%)脉冲闪烁

## 全屏设置
- `main_menu.gd` 的 `_ready()` 添加 `get_window().mode = Window.MODE_FULLSCREEN`

## 挑战模式技能1+Q
- `skill_manager.gd`: 添加 `CHALLENGE_ALLOWED = ["shield", "head_oil"]`，`auto_unlock_challenge_skills()`，`get_visible_skills()`，`activate_by_id()`
- `skill_bar.gd`: Q键绑定head_oil，挑战模式只显示2槽，key_label显示"1"/"Q"
- `game_manager.gd`: 添加 `challenge_mode` 变量
- `main_menu.gd`: 3个游戏入口添加 `auto_unlock_challenge_skills()` 调用
